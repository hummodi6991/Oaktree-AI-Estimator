VITE_API_BASE_URL=REPLACE_ME
# If you’re pointing the UI at the API via the proxy, this local style is fine:
VITE_MAP_STYLE=/osm-style.json
# (optional) If you want to use the public demo style instead, use this:
# VITE_MAP_STYLE=https://demotiles.maplibre.org/style.json
